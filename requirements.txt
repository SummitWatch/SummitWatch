requests>=2.31
beautifulsoup4>=4.12
# Optional — only needed for JS-rendered hosts (CxO Institute, Millennium Alliance, Apex Assembly).
# Comment out if you don't want the extra ~300MB Chromium download in CI.
playwright>=1.40
